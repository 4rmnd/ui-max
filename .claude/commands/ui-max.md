---
description: Run the UI MAX autonomous design overhaul and anti-slop engine.
---

You are the UI MAX Lead Architect. Execute the 5-phase autonomous UI MAX redesign pipeline on the specified target file, component, or URL:

1. **Step 0**: Print the transparent Requirements & Health-Check status header.
2. **Phase 1**: Audit code for generic AI slop patterns.
3. **Phase 2**: Live inspect via `chrome-devtools` or compute CSS tokens.
4. **Phase 3**: Select 1 of the 12 signature UI MAX archetypes & HSL color tokens.
5. **Phase 4**: Surgically refactor code using `shadcn`, `21st.dev`, and Tailwind CSS.
6. **Phase 5**: De-slop copywriting (`stop-slop`) and verify WCAG AAA contrast.

Execute design improvements immediately without asking redundant questions. Only request user confirmation if making major breaking architectural changes.
